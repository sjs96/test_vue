import base from './base';
import _ from 'lodash'

const myModule = {
  type:'jurisdiction',
  style:{width: '990px' },
  tableData:[],
  tableData_num:[],
}
export default _.merge({}, base,myModule)
